#pragma once

#include <vector>
#include <QPainter>
#include <QPen>

/*
class WallsRender {
private:
    struct Wall {
        int x1 = 0, y1 = 0;
        int x2 = 0, y2 = 0;
        int width = 4;
    };
    std::vector<Wall> Walls;

public:
    void wallgen(int x1, int y1, int x2, int y2);
    void draw(QPainter& painter) const;
};
 */